<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.backend.commentanalysis.mapper.UserMapper">
    <insert id="register">
        insert into tb_user(password, phone, username)
        values (#{password}, #{phone}, #{username})
    </insert>
    <!-- 更新用户信息 -->
    <update id="updateUserInfo" parameterType="UserUpdateInfoDTO">
        update tb_user
        set
            username = #{username},
            password = #{newPassword}
        WHERE id = #{id} and password = #{password}
    </update>

    <update id="updateUserPhone" parameterType="UserUpdatePhoneDTO">
        update tb_user
        set
            phone = #{newPhone}
        WHERE id = #{id} and phone = #{phone}
    </update>
</mapper>

